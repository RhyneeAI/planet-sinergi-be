# Gudang Planet — FE Deliverable v2.7

---

## v2.7 (Sudah Rilis)

### Data Model — Transfer Flow

```
┌────────────────────────────────────────────────────────────┐
│                      ADMIN (PUSAT)                         │
├────────────────────────────────────────────────────────────┤
│  OpsExpense (expense_type = MANDOR)                        │
│    ├── mandor_id: (mandor target)                          │
│    ├── transferConfirmation → status: PENDING/CONFIRMED    │
│    └── transferIncome → OpsIncome (created saat confirm)   │
└────────────────────────────────────────────────────────────┘
                           │
                     (mandor confirm)
                           ▼
┌────────────────────────────────────────────────────────────┐
│                      MANDOR                                │
├────────────────────────────────────────────────────────────┤
│  OpsIncome (source_type = MANDOR)                          │
│    ├── mandor_id: (mandor ID)                              │
│    └── muncul di GET /api/v1/incomes (filter by mandor)    │
└────────────────────────────────────────────────────────────┘
```

**Alur:**
1. Admin buat transfer → OpsExpense (MANDOR) + OpsTransferConfirmation (PENDING)
2. Mandor confirm via `POST .../transfer-confirmations/{uuid}/confirm` → OpsIncome terbuat, status jadi CONFIRMED
3. Mandor reject → OpsExpense di-soft-delete, income tidak pernah dibuat

### Yang perlu diketahui FE
- **Income mandor baru terbuat saat confirm** (defer income creation), bukan saat admin buat transfer
- **Status transfer ada di expense** (`transfer_confirmation`), bukan di income
- **Pusat endpoint**: `GET /api/v1/operational/incomes/pusat` dan `/expenses/pusat` (filter INTERNAL + tanpa mandor)
- **DataTablesResponse**: semua index endpoint (`/incomes`, `/expenses`, `/employes`, dll) return `{ data, draw, recordsTotal, recordsFiltered }`, bukan array biasa

---

## v2.7.1

### Dashboard Mandor — Remaining Amount Fix

`GET /api/v1/operational/dashboard/mandor`

`total_expense` sekarang **tidak termasuk** MANDOR-type expenses (transfer dari pusat).

| Sebelum | Sesudah |
|---------|---------|
| `remaining_amount = total_income - total_expense` (termasuk transfer masuk) | `remaining_amount = total_income - total_expense` (hanya expense INTERNAL mandor) |

**Dampak ke FE:** Angka `remaining_amount` di dashboard mandor jadi lebih besar (correct).

---

## v2.7.2

### 1. Expense Detail — `transfer_confirmation` Sekarang Muncul

`GET /api/v1/operational/expenses/{uuid}` dan index — **sebelumnya** `transfer_confirmation` tidak muncul sama sekali di response karena load salah. **Sekarang** muncul:

```json
{
  "data": {
    "uuid": "...",
    "expense_type": "MANDOR",
    "amount": 500000,
    "transfer_confirmation": {
      "uuid": "...",
      "status": "CONFIRMED",
      "confirmed_amount": 500000,
      "confirmed_at": "2026-06-30T10:00:00.000000Z",
      "confirmed_by": { "uuid": "...", "name": "Mandor A" }
    },
    "transfer_income": {
      "uuid": "...",
      "amount": 500000,
      "source_type": "MANDOR"
    }
  }
}
```

**Status:** `"PENDING"` | `"CONFIRMED"` | `"REJECTED"`
**Note:** `transfer_income` null jika status masih PENDING.

### 2. Login — MARKETING & MARKETING_LEAD Diblokir

HTTP 422:
```json
{
  "message": "Akun Anda tidak memiliki akses login.",
  "errors": { "phone": ["Akun Anda tidak memiliki akses login."] }
}
```

---

## v2.7.3

### New Standalone Endpoint — `/api/v1/marketings`

Akses data marketing lintas modul (tanpa prefix `/operational` atau `/pos`).

| Method | Endpoint | Auth | Deskripsi |
|--------|----------|------|-----------|
| GET | `/api/v1/marketings` | SUPERADMIN,OWNER,ADMIN,KEPALA_GUDANG,KEPALA_MANDOR,GUDANG | List marketing |
| GET | `/api/v1/marketings/{uuid}` | sama | Detail marketing + hierarchy |
| POST | `/api/v1/marketings` | SUPERADMIN,ADMIN,KEPALA_GUDANG,KEPALA_MANDOR | Create marketing |
| PUT | `/api/v1/marketings/{uuid}` | sama | Update marketing |
| DELETE | `/api/v1/marketings/{uuid}` | sama | Delete marketing |

**Query params (index):**
- `?role=MARKETING_LEAD` — filter by role
- `?leader_uuid={uuid}` — ambil anggota dari LEAD tertentu
- `?search=` — cari by name/phone/email

**Response (index):**
```json
{
  "data": [
    {
      "uuid": "...",
      "name": "...",
      "role": "MARKETING_LEAD",
      "leader": null,
      "members_count": 5,
      "is_active": true
    },
    {
      "uuid": "...",
      "name": "...",
      "role": "MARKETING",
      "leader": { "uuid": "...", "name": "Leader Name" },
      "members": null,
      "members_count": 0,
      "is_active": true
    }
  ]
}
```

**Response (show — MARKETING_LEAD):**
```json
{
  "data": {
    "uuid": "...",
    "name": "...",
    "role": "MARKETING_LEAD",
    "members": [
      { "uuid": "...", "name": "Anggota 1" },
      { "uuid": "...", "name": "Anggota 2" }
    ],
    "members_count": 2
  }
}
```

**Notes:**
- `leader` hanya muncul jika role = MARKETING
- `members` + `members_count` hanya muncul jika role = MARKETING_LEAD
- Saat create MARKETING, kirim `leader_uuid` untuk assign ke LEAD

### Perbandingan Endpoint Marketing

| Prefix | Hierarchy | Cocok untuk |
|--------|-----------|-------------|
| `/api/v1/marketings` | ✅ leader + members | **Semua modul** ✅ |
| `/api/v1/operational/marketings` | ✅ leader + members | OPS saja |
| `/api/v1/pos/marketings` | ❌ | POS saja (legacy) |

**Rekomendasi:** Pakai `/api/v1/marketings` untuk semua kasus.
